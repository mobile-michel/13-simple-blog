<nav aria-label="Pagination">
<p>{% if pagination.href.previous %}<a href="{{ pagination.href.previous }}"><button class="outline">{{ pagination.page.previous.data.title }}</button></a>{% else %}Nothing more...{% endif %} <- This is page {{ pagination.pageNumber | plus: 1 }} -> {% if pagination.href.next %}<a href="{{ pagination.href.next }}"><button class="outline">{{ pagination.page.next.data.title }}</button></a> {% else %}Nothing more...{% endif %}</p>
</nav>